# Security

Security priorities for DevPath Agent include:

- avoiding accidental exposure of personal candidate data;
- preventing API key leakage through committed environment files;
- separating local sample data from user-provided private inputs;
- introducing privacy masking before future export or sharing flows.

This skeleton includes `.env.example` and ignores `.env`. Real secret handling, privacy enforcement, and export review flows will be implemented later.
