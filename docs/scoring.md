# Scoring

The future scoring system will combine deterministic checks and agent-assisted reasoning. Early deterministic scoring will likely focus on:

- required skill overlap;
- preferred skill overlap;
- project evidence relevance;
- language and location alignment;
- missing-skill prioritization.

The current repository includes only a tiny placeholder overlap utility in `devpath/core/scoring.py`.
